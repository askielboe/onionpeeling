proc comparespec { args } {
    # This procedure will compare output from genspectra2 with real data and determine the fit parameter.
    # In practice it will subtract the two spectra and fit to the constant function f(x) = 0 returning the reduced-chi-square.
}